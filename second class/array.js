// what is array ?

let name = "Keshav";  // String
let age = 19;   // Number
let val = true;   // Boolean

let data = [ "Keshav", 19, true ]

// console.log(data[-3]) --> This will not run. Why ? Because javascript does not support negative indexing. It only supports left to right approach of indexing. But array has characteristics of both positive and negative indexing.


console.log(data[0]) // This will work